#include "header.h"

int main()
{
    int a = 10, b = 5;

    // for reversing num
    int num = 123456789;

    // // by address
    // cout << "\n before swap a & b :  " << a << " " << b;
    // swap_by_add(&a, &b);

    // cout << "\n After swap a & b :  " << a << " " << b << endl;

    // // by reference
    // cout << "\n before swap a & b :  " << a << " " << b;
    // swap_by_ref(a, b);

    // cout << "\n After swap a & b :  " << a << " " << b << endl;

    // qestion 3 -------------------------------------------

    // sum_and_diff(a, b);

    // question 4 -------------------------------

    reverse_num(num);

    return 0;
}